{
  "name": "afterlight",
  "version": "0.1.0",
  "private": true,
  "description": "AFTERLIGHT, a dependency-free browser game",
  "scripts": {
    "serve": "node tools/server.cjs",
    "pack": "node tools/pack.cjs",
    "test:engine": "node --test tests/engine.test.cjs",
    "test:browser": "node tests/browser.spec.cjs",
    "test": "node --test tests/*.test.cjs && node tests/browser.spec.cjs && node tests/interaction.spec.cjs && node tests/campaign-browser.cjs",
    "test:performance": "node tests/performance.cjs",
    "test:all": "npm test && npm run test:performance"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
